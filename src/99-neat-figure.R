#' Two panel GIF that shows on the left, a map for one BBS route (so 50 poiints)
#' which changing landcover over time, and on the Right a plot that shows the change
#' in expected detectability for a given species